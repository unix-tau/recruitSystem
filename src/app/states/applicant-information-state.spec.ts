import { ApplicantInformationState } from './applicant-information-state';

describe('ApplicantInformationState', () => {
  it('should create an instance', () => {
    expect(new ApplicantInformationState()).toBeTruthy();
  });
});
