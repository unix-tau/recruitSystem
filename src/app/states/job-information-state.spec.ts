import { JobInformationState } from './job-information-state';

describe('JobInformationState', () => {
  it('should create an instance', () => {
    expect(new JobInformationState()).toBeTruthy();
  });
});
