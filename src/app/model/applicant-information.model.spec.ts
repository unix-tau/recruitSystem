import { ApplicantInformation } from './applicant-information.model';

describe('ApplicantInformation', () => {
  it('should create an instance', () => {
    expect(new ApplicantInformation()).toBeTruthy();
  });
});
